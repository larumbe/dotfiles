## bind z exec "$HOME/.kbdlayout"

exec unclutter -idle 5 -jitter 2 -root &

bind z exec ibus-daemon
bind Z exec killall ibus-daemon

bind o focus
bind n focusdown
bind p focusup
bind f focusright
bind b focusleft

bind h exec bash $HOME/pantalla.sh
bind q only
bind e exec
bind C-c exec urxvt

# Get rid of that ugly crosshairs default cursor
# and set the background to black

# Use name of programme rathern than title in the window list
defwinname name






